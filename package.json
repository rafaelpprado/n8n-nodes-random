{
  "name": "n8n-nodes-random",
  "version": "1.0.0",
  "description": "Random.org True Random Number Generator node for n8n",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/postbuild.mjs",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "n8n-workflow": "^1.60.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.0"
  },
  "n8n": {
    "nodes": [
      "dist/nodes/Random.node.js"
    ]
  }
}
